<nav>
    <ol n:inner-foreach="$breadcrumb as $menu">
        <li>
            <a n:tag-if="!$iterator->last" href="{plink $menu->link id=>$menu->linkId}">
                {$menu->title|translate}
            </a>
        </li>
    </ol>
</nav>