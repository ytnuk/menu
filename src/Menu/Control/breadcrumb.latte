<nav>
    <ol n:inner-foreach="$menu as $menu">
        <li>
            <a n:tag-if="!$iterator->last" href="{plink $menu->link id=>$menu->link_id}">
                {$menu->title|translate}
            </a>
        </li>
    </ol>
</nav>