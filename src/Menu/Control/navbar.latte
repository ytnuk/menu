<nav class="navbar navbar-default navbar-static-top">
	<div class="container">
		<header class="navbar-header">
			<button class="navbar-toggle" data-toggle="collapse" data-target=".navbar-{$menu->id}-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
		</header>
		<div class="collapse navbar-collapse navbar-{$menu->id}-collapse">
			<ul n:class="nav,navbar-nav">
				<li n:class="$presenter->isLinkCurrent($menu->link) ? active">
					<a n:href="$menu->link">
						<span class="glyphicon glyphicon-home"></span>
						{$menu->title|translate}
					</a>
				</li>
				<li n:foreach="$menu->children as $menu" n:class="isset($control[$menu->id])?active,count($menu->children)?dropdown">
					<a n:href="$menu->link" n:attr="data-toggle=>count($menu->children)?dropdown" n:class="count($menu->children)?dropdown-toggle">
						{$menu->title|translate}
						<b class="caret" n:if="count($menu->children)"></b>
					</a>
					<ul n:if="count($menu->children)" class="dropdown-menu">
						<li n:class="$presenter->isLinkCurrent($menu->link) ? active">
							<a n:href="$menu->link">
								<b class="caret-down"></b>
								{$menu->title|translate}
							</a>
						</li>
						<li n:class="isset($control[$menu->id])?active" n:foreach="$menu->children as $menu">
							<a n:href="$menu->link"> {$menu->title|translate}</a>
						</li>
					</ul>
				</li>
			</ul>
		</div>
	</div>
</nav>
